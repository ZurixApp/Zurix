# Server Configuration
PORT=3001
NODE_ENV=development

# Solana Configuration
# Option 1: Use Helius (Recommended - better performance and reliability)
HELIUS_API_KEY=your-helius-api-key-here
SOLANA_NETWORK=mainnet
# For devnet testing, set SOLANA_NETWORK=devnet

# Option 2: Use custom RPC URL (if not using Helius)
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# For devnet: https://api.devnet.solana.com

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/privacy_swap

# Encryption (32-byte key for AES-256) - Generate a random 32-byte hex string
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-byte-encryption-key-here-change-this-in-production

# Redis (for BullMQ job queue) - Optional, can use in-memory for development
REDIS_URL=redis://localhost:6379

# Optional: AWS KMS for key management (production)
# AWS_KMS_KEY_ID=arn:aws:kms:region:account:key/key-id
# AWS_REGION=us-east-1

# Relayer Fee Configuration
# Wallet address that receives the 0.05% relayer fee from each transaction
# If not set, fees will be tracked in database but not automatically sent
RELAYER_FEE_WALLET=your-relayer-fee-wallet-address-here

# Treasury Wallet Configuration
# Private key (base58 encoded) of wallet that funds intermediate wallets with transaction fees
# Generate with: solana-keygen new --outfile treasury.json && cat treasury.json | jq -r .secretKey | base58
# This wallet should have SOL to cover transaction fees (0.0001 SOL per intermediate wallet)
# If not set, intermediate wallets will be funded from previous wallets in the chain
TREASURY_WALLET_PRIVATE_KEY=your-treasury-wallet-private-key-base58-here

